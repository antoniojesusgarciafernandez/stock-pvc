name: pvc_stock_manager
description: Gestión de stock e inventario de perfiles de PVC para taller.

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.3
  path_provider: ^2.1.4
  path: ^1.9.0
  provider: ^6.1.2
  intl: ^0.19.0
  # --- VERSIONES CORRECTAS ---
  sqflite_common_ffi: ^2.3.0+2
  sqflite_common_ffi_web: ^0.4.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true